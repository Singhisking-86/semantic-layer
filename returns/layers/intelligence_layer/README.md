# Intelligence layer (TARGET DESIGN)

Placeholders. Consumes ONLY the Data layer's tier-2 surfaces + semantic catalog,
never source systems. Design in ../../docs/00_vision_target_architecture.md.

- `services/`       — insight discovery, problem analysis, action recommendation, action impact assessment
- `skills/`         — versioned, evaluated analytical skills (definition + eval set + score history)
- `action_catalog/` — catalogue of recommendable actions + expected-effect models + HIL→agentic ladder state
