# Semantic Catalog (agent-readable)

The surface the Intelligence + Application layers read. One entry per entity and
metric: business definition, derivation (in tier-1 terms), caveats, freshness,
version. No Teradata/Databricks names here — those live in tier-3 bindings.
Format TBD (YAML/JSON/markdown); keep it generated from the registry where possible.
